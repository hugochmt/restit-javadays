{
  "name": "restit-javadays",
  "version": "1.0.0",
  "description": "Support pour la restit javadays",
  "scripts": {
    "restit-serve": "marp -ps",
    "restit-watch": "cpy \"assets/**/*\" dist/assets && marp -pw"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@marp-team/marp-cli": "^3.0.2",
    "cpy-cli": "^5.0.0"
  }
}
